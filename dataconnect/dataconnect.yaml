specVersion: "v1beta"
serviceId: "multiparty"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "multiparty-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
